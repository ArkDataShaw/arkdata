/** Stub — pixel-only API endpoint functions (not yet implemented) */

export async function adminGetDataHygieneState(tenantId) {
  return { mode: "normal", quarantinedCount: 0, lastCleanup: null };
}
